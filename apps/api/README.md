# `apps/api`

Reserved for the future synchronous application API.

This runtime should not land until the onboarding flows need durable server-side
validation, orchestration, or state changes that cannot stay in the browser.
